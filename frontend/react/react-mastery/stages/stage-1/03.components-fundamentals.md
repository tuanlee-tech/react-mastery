# 📅 NGÀY 3: Components Fundamentals

## 🎯 Mục tiêu hôm nay
- Hiểu sâu về Function Components
- Mastering Props: passing data, prop types, default props
- Props Destructuring cho code sạch hơn
- Children prop và component composition
- Prop Drilling: vấn đề và giải pháp cơ bản

---

## 📚 PHẦN 1: THEORY (30-45 phút)

### **1.1. Function Components**

Function component là function JavaScript return React elements.

```jsx
// Basic function component
function Welcome() {
  return <h1>Hello, World!</h1>;
}

// Arrow function component
const Welcome = () => {
  return <h1>Hello, World!</h1>;
};

// Implicit return (no curly braces)
const Welcome = () => <h1>Hello, World!</h1>;
```

**Best Practice:**
- Tên component viết hoa (PascalCase)
- Một component một file
- Export default ở cuối file

---

### **1.2. Props - Passing Data**

Props (properties) là cách truyền data từ parent → child component.

#### **Basic Props**

```jsx
// Parent component
function App() {
  return <Greeting name="Alice" age={25} />;
}

// Child component
function Greeting(props) {
  return (
    <div>
      <h1>Hello, {props.name}!</h1>
      <p>You are {props.age} years old.</p>
    </div>
  );
}
```

#### **Props Destructuring**

```jsx
// ✅ Destructuring trong parameters (khuyên dùng)
function Greeting({ name, age }) {
  return (
    <div>
      <h1>Hello, {name}!</h1>
      <p>You are {age} years old.</p>
    </div>
  );
}

// ✅ Destructuring trong body
function Greeting(props) {
  const { name, age } = props;
  return (
    <div>
      <h1>Hello, {name}!</h1>
      <p>You are {age} years old.</p>
    </div>
  );
}
```

#### **Default Props**

```jsx
// Cách 1: Default parameters (modern, khuyên dùng)
function Button({ text = 'Click me', variant = 'primary' }) {
  return <button className={variant}>{text}</button>;
}

// Cách 2: defaultProps (legacy)
function Button({ text, variant }) {
  return <button className={variant}>{text}</button>;
}

Button.defaultProps = {
  text: 'Click me',
  variant: 'primary'
};
```

#### **Props Types**

Có thể pass mọi kiểu dữ liệu:

```jsx
function ComponentDemo() {
  const user = { name: 'John', age: 30 };
  const colors = ['red', 'green', 'blue'];
  const handleClick = () => console.log('clicked');

  return (
    <MyComponent
      // Primitives
      stringProp="hello"
      numberProp={42}
      booleanProp={true}
      
      // Objects
      objectProp={user}
      arrayProp={colors}
      
      // Functions
      onAction={handleClick}
      
      // JSX
      renderItem={<div>Custom content</div>}
    />
  );
}
```

**⚠️ Props are Read-Only (Immutable):**

```jsx
// ❌ KHÔNG BAO GIỜ modify props
function Button({ text }) {
  text = 'Modified'; // ❌ Error: Assignment to constant
  return <button>{text}</button>;
}

// ✅ Dùng local state nếu cần modify
function Button({ initialText }) {
  const [text, setText] = useState(initialText);
  return <button onClick={() => setText('Clicked')}>{text}</button>;
}
```

---

### **1.3. Children Prop**

`children` là prop đặc biệt chứa content giữa opening và closing tags.

#### **Basic Children**

```jsx
// Parent
function App() {
  return (
    <Card>
      <h2>Title</h2>
      <p>This is the content</p>
    </Card>
  );
}

// Child component
function Card({ children }) {
  return (
    <div className="card">
      {children}
    </div>
  );
}
```

#### **Children với Props khác**

```jsx
function Card({ title, children, footer }) {
  return (
    <div className="card">
      <div className="card-header">
        <h3>{title}</h3>
      </div>
      <div className="card-body">
        {children}
      </div>
      {footer && (
        <div className="card-footer">
          {footer}
        </div>
      )}
    </div>
  );
}

// Usage
<Card 
  title="My Card"
  footer={<button>Action</button>}
>
  <p>Card content goes here</p>
  <p>Multiple children allowed</p>
</Card>
```

#### **Manipulating Children**

```jsx
import React from 'react';

function List({ children }) {
  // Count children
  const count = React.Children.count(children);
  
  // Map over children
  const items = React.Children.map(children, (child, index) => (
    <li key={index}>{child}</li>
  ));

  return (
    <div>
      <p>Total items: {count}</p>
      <ul>{items}</ul>
    </div>
  );
}

// Usage
<List>
  <span>Item 1</span>
  <span>Item 2</span>
  <span>Item 3</span>
</List>
```

---

### **1.4. Component Composition**

Composition là cách xây dựng complex components từ simple components.

#### **Container/Content Pattern**

```jsx
function Dialog({ title, children, actions }) {
  return (
    <div className="dialog">
      <div className="dialog-title">{title}</div>
      <div className="dialog-content">{children}</div>
      <div className="dialog-actions">{actions}</div>
    </div>
  );
}

// Usage
<Dialog
  title="Confirm Action"
  actions={
    <>
      <button>Cancel</button>
      <button>Confirm</button>
    </>
  }
>
  <p>Are you sure you want to delete this item?</p>
</Dialog>
```

#### **Slot Pattern (Named Children)**

```jsx
function Layout({ header, sidebar, content, footer }) {
  return (
    <div className="layout">
      <header>{header}</header>
      <div className="main">
        <aside>{sidebar}</aside>
        <main>{content}</main>
      </div>
      <footer>{footer}</footer>
    </div>
  );
}

// Usage
<Layout
  header={<nav>Navigation</nav>}
  sidebar={<Menu />}
  content={<ArticleList />}
  footer={<p>© 2024</p>}
/>
```

#### **Specialization Pattern**

```jsx
// Generic component
function Button({ variant = 'default', children, ...props }) {
  return (
    <button className={`btn btn-${variant}`} {...props}>
      {children}
    </button>
  );
}

// Specialized components
function PrimaryButton(props) {
  return <Button variant="primary" {...props} />;
}

function DangerButton(props) {
  return <Button variant="danger" {...props} />;
}

// Usage
<PrimaryButton onClick={handleSave}>Save</PrimaryButton>
<DangerButton onClick={handleDelete}>Delete</DangerButton>
```

---

### **1.5. Prop Drilling**

Prop drilling là vấn đề khi pass props qua nhiều levels components.

#### **Vấn đề:**

```jsx
// Level 0: App
function App() {
  const user = { name: 'John', role: 'admin' };
  return <Dashboard user={user} />;
}

// Level 1: Dashboard (không dùng user, chỉ pass tiếp)
function Dashboard({ user }) {
  return <Sidebar user={user} />;
}

// Level 2: Sidebar (không dùng user, chỉ pass tiếp)
function Sidebar({ user }) {
  return <UserMenu user={user} />;
}

// Level 3: UserMenu (cuối cùng mới dùng user)
function UserMenu({ user }) {
  return <div>Hello, {user.name}</div>;
}
```

#### **Giải pháp 1: Component Composition**

```jsx
function App() {
  const user = { name: 'John', role: 'admin' };
  
  return (
    <Dashboard>
      <Sidebar>
        <UserMenu user={user} />
      </Sidebar>
    </Dashboard>
  );
}

// Components không cần biết về user
function Dashboard({ children }) {
  return <div className="dashboard">{children}</div>;
}

function Sidebar({ children }) {
  return <aside>{children}</aside>;
}

function UserMenu({ user }) {
  return <div>Hello, {user.name}</div>;
}
```

#### **Giải pháp 2: Spread Operator (khi cần pass nhiều props)**

```jsx
function UserCard(props) {
  return <UserAvatar {...props} />;
}

function UserAvatar({ name, avatar, status }) {
  return (
    <div>
      <img src={avatar} alt={name} />
      <span>{name}</span>
      <span>{status}</span>
    </div>
  );
}

// Usage
<UserCard name="John" avatar="..." status="online" />
```

**Ngày 4 sẽ học Context API để giải quyết triệt để prop drilling!**

---

### **1.6. Props Best Practices**

#### **1. Destructuring với default values**

```jsx
// ✅ Clean and readable
function Card({ 
  title = 'Untitled',
  description = '',
  image = '/default.jpg',
  onAction = () => {}
}) {
  return (
    <div>
      <img src={image} alt={title} />
      <h3>{title}</h3>
      <p>{description}</p>
      <button onClick={onAction}>Action</button>
    </div>
  );
}
```

#### **2. Rest parameters cho flexibility**

```jsx
function Input({ label, error, ...inputProps }) {
  return (
    <div>
      <label>{label}</label>
      <input {...inputProps} />
      {error && <span className="error">{error}</span>}
    </div>
  );
}

// Usage - tất cả props còn lại đều pass vào <input>
<Input 
  label="Email"
  type="email"
  placeholder="Enter email"
  required
  maxLength={50}
/>
```

#### **3. Object props cho related data**

```jsx
// ❌ Too many props
function UserProfile({ name, email, avatar, age, city, country, bio }) {
  // ...
}

// ✅ Grouped props
function UserProfile({ user, address, bio }) {
  return (
    <div>
      <h2>{user.name}</h2>
      <p>{user.email}</p>
      <p>{address.city}, {address.country}</p>
      <p>{bio}</p>
    </div>
  );
}

// Usage
<UserProfile
  user={{ name: 'John', email: 'john@example.com' }}
  address={{ city: 'Hanoi', country: 'Vietnam' }}
  bio="Developer"
/>
```

#### **4. Boolean props shorthand**

```jsx
// ✅ Shorthand cho true
<Button disabled />
<Input required />
<Modal isOpen />

// Tương đương với:
<Button disabled={true} />
<Input required={true} />
<Modal isOpen={true} />
```

#### **5. Callback props naming**

```jsx
// Convention: on[EventName]
function SearchInput({ onSearch, onChange, onClear }) {
  return (
    <div>
      <input onChange={onChange} />
      <button onClick={onSearch}>Search</button>
      <button onClick={onClear}>Clear</button>
    </div>
  );
}
```

---

## 💻 PHẦN 2: CODE DEMO (30-45 phút)

### **Demo: Component Library**

```jsx
// Button.jsx
function Button({ 
  children,
  variant = 'primary',
  size = 'medium',
  disabled = false,
  onClick,
  ...props 
}) {
  const baseClass = 'btn';
  const variantClass = `btn-${variant}`;
  const sizeClass = `btn-${size}`;
  
  return (
    <button
      className={`${baseClass} ${variantClass} ${sizeClass}`}
      disabled={disabled}
      onClick={onClick}
      {...props}
    >
      {children}
    </button>
  );
}

export default Button;
```

```jsx
// Card.jsx
function Card({ 
  title,
  subtitle,
  image,
  children,
  footer,
  variant = 'default'
}) {
  return (
    <div className={`card card-${variant}`}>
      {image && (
        <div className="card-image">
          <img src={image} alt={title} />
        </div>
      )}
      
      <div className="card-header">
        <h3>{title}</h3>
        {subtitle && <p className="subtitle">{subtitle}</p>}
      </div>
      
      {children && (
        <div className="card-body">
          {children}
        </div>
      )}
      
      {footer && (
        <div className="card-footer">
          {footer}
        </div>
      )}
    </div>
  );
}

export default Card;
```

```jsx
// Badge.jsx
function Badge({ 
  children,
  variant = 'default',
  size = 'medium',
  rounded = false
}) {
  return (
    <span 
      className={`
        badge 
        badge-${variant} 
        badge-${size} 
        ${rounded ? 'badge-rounded' : ''}
      `}
    >
      {children}
    </span>
  );
}

export default Badge;
```

```jsx
// Avatar.jsx
function Avatar({ 
  src,
  alt = 'User avatar',
  size = 'medium',
  status,
  fallback
}) {
  return (
    <div className={`avatar avatar-${size}`}>
      {src ? (
        <img src={src} alt={alt} />
      ) : (
        <div className="avatar-fallback">
          {fallback || alt.charAt(0).toUpperCase()}
        </div>
      )}
      
      {status && (
        <span className={`avatar-status avatar-status-${status}`} />
      )}
    </div>
  );
}

export default Avatar;
```

```jsx
// App.jsx - Demo usage
import Button from './Button';
import Card from './Card';
import Badge from './Badge';
import Avatar from './Avatar';

function App() {
  return (
    <div className="app">
      <h1>Component Library Demo</h1>
      
      {/* Buttons */}
      <section>
        <h2>Buttons</h2>
        <Button variant="primary">Primary</Button>
        <Button variant="secondary">Secondary</Button>
        <Button variant="danger">Danger</Button>
        <Button disabled>Disabled</Button>
        <Button size="small">Small</Button>
        <Button size="large">Large</Button>
      </section>

      {/* Cards */}
      <section>
        <h2>Cards</h2>
        <Card
          title="Product Card"
          subtitle="$99.99"
          image="https://via.placeholder.com/300x200"
          footer={
            <>
              <Button variant="primary">Buy Now</Button>
              <Button variant="secondary">Add to Cart</Button>
            </>
          }
        >
          <p>This is a great product that you should definitely buy!</p>
          <Badge variant="success">In Stock</Badge>
        </Card>
      </section>

      {/* Badges */}
      <section>
        <h2>Badges</h2>
        <Badge variant="primary">Primary</Badge>
        <Badge variant="success">Success</Badge>
        <Badge variant="warning">Warning</Badge>
        <Badge variant="danger">Danger</Badge>
        <Badge rounded>Rounded</Badge>
      </section>

      {/* Avatars */}
      <section>
        <h2>Avatars</h2>
        <Avatar 
          src="https://i.pravatar.cc/150?img=1"
          alt="John Doe"
          status="online"
        />
        <Avatar 
          alt="Jane Smith"
          fallback="JS"
          status="away"
        />
        <Avatar 
          src="https://i.pravatar.cc/150?img=3"
          size="large"
          status="offline"
        />
      </section>
    </div>
  );
}

export default App;
```

---

## 🔨 PHẦN 3: THỰC HÀNH (60-90 phút)

### **Exercise 1: User Profile Components**

Tạo hệ thống user profile với các components:

```jsx
// UserProfile.jsx
function UserProfile({ user }) {
  // TODO:
  // Render user profile với:
  // - UserHeader (avatar, name, bio)
  // - UserStats (posts, followers, following)
  // - UserPosts (list of posts)
  
  return (
    <div className="user-profile">
      {/* Your code */}
    </div>
  );
}

// UserHeader.jsx
function UserHeader({ avatar, name, username, bio, isVerified }) {
  // TODO:
  // - Avatar component
  // - Name với verified badge nếu isVerified
  // - Username (@username)
  // - Bio text
  // - Follow/Unfollow button
  
  return (
    <div className="user-header">
      {/* Your code */}
    </div>
  );
}

// UserStats.jsx
function UserStats({ posts, followers, following }) {
  // TODO:
  // Hiển thị 3 stats trong grid
  // Format numbers với commas (1,234)
  // Clickable stats
  
  return (
    <div className="user-stats">
      {/* Your code */}
    </div>
  );
}

// UserPosts.jsx
function UserPosts({ posts }) {
  // TODO:
  // Render list of PostCard components
  // Empty state nếu không có posts
  
  return (
    <div className="user-posts">
      {/* Your code */}
    </div>
  );
}

// PostCard.jsx
function PostCard({ post }) {
  // TODO:
  // - Post image/content
  // - Like, comment, share buttons
  // - Timestamp
  
  return (
    <div className="post-card">
      {/* Your code */}
    </div>
  );
}

// Demo data
const sampleUser = {
  id: 1,
  name: 'John Doe',
  username: 'johndoe',
  avatar: 'https://i.pravatar.cc/150?img=1',
  bio: 'Software Developer | React Enthusiast',
  isVerified: true,
  stats: {
    posts: 145,
    followers: 2340,
    following: 890
  },
  posts: [
    {
      id: 1,
      content: 'Just launched my new project!',
      image: 'https://via.placeholder.com/400',
      likes: 89,
      comments: 12,
      timestamp: '2 hours ago'
    },
    // More posts...
  ]
};
```

### **Exercise 2: E-commerce Product Components**

```jsx
// ProductGrid.jsx
function ProductGrid({ products, onAddToCart }) {
  // TODO:
  // Render grid of ProductCard components
  // Pass onAddToCart down
  
  return (
    <div className="product-grid">
      {/* Your code */}
    </div>
  );
}

// ProductCard.jsx
function ProductCard({ product, onAddToCart }) {
  // Props: id, name, price, image, rating, inStock, discount
  
  // TODO:
  // - Product image
  // - Product name
  // - Price (hiển thị giá cũ nếu có discount)
  // - Rating stars
  // - Add to Cart button (disabled nếu out of stock)
  // - Discount badge nếu có
  // - Out of Stock overlay nếu !inStock
  
  return (
    <div className="product-card">
      {/* Your code */}
    </div>
  );
}

// ProductImage.jsx
function ProductImage({ src, alt, discount, inStock }) {
  // TODO:
  // - Image với fallback
  // - Discount badge overlay
  // - Out of stock overlay
  
  return (
    <div className="product-image">
      {/* Your code */}
    </div>
  );
}

// ProductPrice.jsx
function ProductPrice({ price, originalPrice, currency = 'USD' }) {
  // TODO:
  // - Current price (large, bold)
  // - Original price (strikethrough) nếu có discount
  // - Savings amount
  
  return (
    <div className="product-price">
      {/* Your code */}
    </div>
  );
}

// Rating.jsx
function Rating({ rating, reviews, size = 'medium' }) {
  // TODO:
  // - Star icons (filled/half/empty)
  // - Rating number (4.5)
  // - Reviews count (128 reviews)
  
  return (
    <div className="rating">
      {/* Your code */}
    </div>
  );
}
```

### **Exercise 3: Dashboard Layout**

```jsx
// Dashboard.jsx
function Dashboard() {
  // TODO:
  // Layout với:
  // - Sidebar (navigation)
  // - Header (user menu, notifications)
  // - Main content area
  // - Footer
  
  return (
    <div className="dashboard">
      {/* Your code */}
    </div>
  );
}

// Sidebar.jsx
function Sidebar({ items, activeItem, onItemClick }) {
  // TODO:
  // - Logo
  // - Navigation items
  // - Active state
  // - Icons cho mỗi item
  
  return (
    <aside className="sidebar">
      {/* Your code */}
    </aside>
  );
}

// Header.jsx
function Header({ user, notifications, onProfileClick, onNotificationClick }) {
  // TODO:
  // - Search bar
  // - Notification bell với badge count
  // - User menu dropdown
  
  return (
    <header className="header">
      {/* Your code */}
    </header>
  );
}

// StatCard.jsx
function StatCard({ title, value, icon, trend, trendValue }) {
  // TODO:
  // - Icon
  // - Title
  // - Large value number
  // - Trend indicator (up/down arrow với color)
  // - Trend percentage
  
  return (
    <div className="stat-card">
      {/* Your code */}
    </div>
  );
}

// Demo usage
const dashboardData = {
  user: {
    name: 'Admin User',
    avatar: 'https://i.pravatar.cc/150?img=5'
  },
  sidebarItems: [
    { id: 'dashboard', label: 'Dashboard', icon: '📊' },
    { id: 'products', label: 'Products', icon: '📦' },
    { id: 'orders', label: 'Orders', icon: '🛒' },
    { id: 'customers', label: 'Customers', icon: '👥' },
    { id: 'analytics', label: 'Analytics', icon: '📈' }
  ],
  stats: [
    { title: 'Total Revenue', value: '$45,231', icon: '💰', trend: 'up', trendValue: '12%' },
    { title: 'Orders', value: '1,234', icon: '🛒', trend: 'up', trendValue: '8%' },
    { title: 'Customers', value: '5,678', icon: '👥', trend: 'down', trendValue: '3%' },
    { title: 'Products', value: '890', icon: '📦', trend: 'up', trendValue: '5%' }
  ]
};
```

### **Exercise 4: Form Components (Challenge)**

```jsx
// Form.jsx
function Form({ onSubmit, children }) {
  // TODO:
  // - Handle form submission
  // - Prevent default
  // - Pass handleSubmit down via children composition
  
  return (
    <form onSubmit={onSubmit}>
      {children}
    </form>
  );
}

// FormField.jsx
function FormField({ label, error, required, children }) {
  // TODO:
  // - Label với required indicator
  // - Children (input component)
  // - Error message
  
  return (
    <div className="form-field">
      {/* Your code */}
    </div>
  );
}

// Input.jsx
function Input({ 
  type = 'text',
  placeholder,
  value,
  onChange,
  error,
  icon,
  ...props 
}) {
  // TODO:
  // - Input với all native props
  // - Icon (left/right)
  // - Error state styling
  
  return (
    <div className="input-wrapper">
      {/* Your code */}
    </div>
  );
}

// Select.jsx
function Select({ options, value, onChange, placeholder, ...props }) {
  // TODO:
  // - Select với options
  // - Placeholder option
  // - Custom styling
  
  return (
    <select value={value} onChange={onChange} {...props}>
      {/* Your code */}
    </select>
  );
}

// Checkbox.jsx
function Checkbox({ label, checked, onChange, ...props }) {
  // TODO:
  // - Custom styled checkbox
  // - Label
  
  return (
    <label className="checkbox">
      {/* Your code */}
    </label>
  );
}

// Usage Example
function RegistrationForm() {
  return (
    <Form onSubmit={handleSubmit}>
      <FormField label="Email" required error={errors.email}>
        <Input
          type="email"
          placeholder="Enter your email"
          icon="📧"
          value={email}
          onChange={handleEmailChange}
        />
      </FormField>
      
      <FormField label="Password" required>
        <Input
          type="password"
          placeholder="Enter password"
          icon="🔒"
          value={password}
          onChange={handlePasswordChange}
        />
      </FormField>
      
      <FormField label="Country">
        <Select
          options={countries}
          value={country}
          onChange={handleCountryChange}
          placeholder="Select country"
        />
      </FormField>
      
      <Checkbox
        label="I agree to terms and conditions"
        checked={agreedToTerms}
        onChange={handleTermsChange}
      />
      
      <Button type="submit" variant="primary">
        Register
      </Button>
    </Form>
  );
}
```

---

## ✅ PHẦN 4: REVIEW & CHECKLIST (15-30 phút)

### **Function Components:**
- [ ] Viết được function components
- [ ] PascalCase naming convention
- [ ] Return JSX elements

### **Props:**
- [ ] Pass props từ parent → child
- [ ] Destructuring props
- [ ] Default values cho props
- [ ] Props are immutable
- [ ] Rest/spread operators
- [ ] Boolean props shorthand

### **Children Prop:**
- [ ] Hiểu children prop
- [ ] Composition pattern
- [ ] Slot pattern (named children)
- [ ] React.Children utilities

### **Component Composition:**
- [ ] Container/Content pattern
- [ ] Specialization pattern
- [ ] Build complex components from simple ones

### **Prop Drilling:**
- [ ] Nhận biết prop drilling problem
- [ ] Giải quyết với composition
- [ ] Biết khi nào cần Context (ngày 4)

### **Common Mistakes:**

```jsx
// ❌ Modify props
function Component({ data }) {
  data.name = 'New'; // NEVER DO THIS
}

// ❌ Missing children handling
function Card({ children }) {
  return <div>{children.text}</div>; // children might not have .text
}

// ❌ Not using default values
function Button({ text }) {
  return <button>{text}</button>; // Undefined if text not passed
}

// ✅ ĐÚNG
function Component({ data }) {
  const [localData, setLocalData] = useState(data);
  setLocalData({ ...localData, name: 'New' });
}

function Card({ children }) {
  return <div>{children}</div>;
}

function Button({ text = 'Click me' }) {
  return <button>{text}</button>;
}
```

---

## 🎯 HOMEWORK

### **1. Blog Post Components**
Tạo complete blog system với:
- `BlogPost` (main container)
- `PostHeader` (title, author, date, tags)
- `PostContent` (text, images, code blocks)
- `PostFooter` (likes, comments, share)
- `AuthorCard` (avatar, name, bio, follow button)
- `CommentList` & `CommentItem`
- `RelatedPosts`

### **2. Advanced Product Components**
Extend product system với:
- `ProductDetails` (detailed view)
- `ProductGallery` (multiple images with thumbnails)
- `ProductVariants` (size, color selection)
- `ProductReviews` (rating, reviews list)
- `ProductQuantity` (+ - controls)
- `AddToCartButton` (with loading state)

### **3. Notification System**
Tạo notification components:
- `NotificationCenter` (container)
- `NotificationList`
- `NotificationItem` (different types: info, success, warning, error)
- `NotificationBadge` (unread count)
- Toast notifications

### **4. Đọc thêm:**
- [React Docs - Passing Props to a Component](https://react.dev/learn/passing-props-to-a-component)
- [React Docs - Composition vs Inheritance](https://react.dev/learn/composition-vs-inheritance)

---

## 📝 Key Takeaways

1. **Props**: Read-only data flow từ parent → child
2. **Children**: Powerful pattern cho component composition
3. **Destructuring**: Makes code cleaner and more readable
4. **Composition**: Build complex UIs from simple components
5. **Reusability**: DRY principle - Don't Repeat Yourself

---

**🚀 Ngày mai:** Component Composition Deep Dive - Compound Components, Render Props, và advanced patterns!